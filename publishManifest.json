{
  "$schema": "http://json.schemastore.org/vsix-publish",
  "categories": [ "security", "build", "setup and deployment" ], // The categories of the extension. Between 1 and 3 categories are required.
  "tags": [ "code protection", "code security", "data protection", "encryption", "obfuscation", "obfuscator", "vulnerabilities", ".NET", ".NET Core", ".NET MAUI", ".NET5", ".NET6", ".NET7", "Application Hardening", "Application Protection", "Application Security", "Application Shielding", "Packer", "security", "uwp", "xamarin", "code obfuscation", "shield", "app shielding", "app shield" ],
  "identity": {
    "internalName": "ByteHideShieldVS22" // If not specified, we try to generate the name from the display name of the extension in the vsixmanifest file.
    // Required if the display name is not the actual name of the extension.
  },
  "overview": "overview.md", // Path to the "readme" file that gets uploaded to the Marketplace. Required.
  "priceCategory": "free", // Either "free", "trial", or "paid". Defaults to "free".
  "publisher": "ByteHide", // The name of the publisher. Required.
  "private": false, // Specifies whether or not the extension should be public when uploaded. Defaults to false.
  "qna": true, // Specifies whether or not the extension should have a Q&A section. Defaults to true.
  "repo": "https://github.com/dotnetsafer/Shield.VisualStudio.Extension" // Not required.
}
